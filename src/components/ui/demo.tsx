import { LayoutPreloader } from "./layout-preloader";

/**
 * Demo entry for the layout-preloader registry pattern.
 * Production usage: import { LayoutPreloader } from "@/components/ui/layout-preloader"
 * and place it as the page hero (see src/app/page.tsx).
 */
export default function DefaultDemo() {
  return <LayoutPreloader />;
}
